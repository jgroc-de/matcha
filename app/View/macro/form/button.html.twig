{% macro button(type, value) %}
<input class="w3-button w3-ripple w3-theme-l2 w3-hover-green w3-margin-left w3-margin-bottom"
       type={{ type }}
       value={{ value|default(type) }}
       style="float:right">
{% endmacro %}
