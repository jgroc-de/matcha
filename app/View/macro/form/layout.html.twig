{% macro form(inputs, h2, action, flash, show) %}
{% from 'macro/form/title.html.twig' import title %}

{{ title(h2, action,  show) }}
<form id="{{ action }}" class="w3-container w3-theme-light clearfix w3-hide {% if show %}w3-show{% endif %}"
      style="opacity:0.95" action="{{ path_for(action) }}" method="post">
    {% if show %}
        {% for key, message in flash %}
            <div class="w3-panel w3-{% if key == "success" %}green{% else %}red{% endif %}">
                <p>{{ message }}</p>
            </div>
        {% endfor %}
    {% endif %}
    {{ inputs }}
</form>
{% endmacro %}
