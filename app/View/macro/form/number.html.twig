{% macro number(name, id, min, max) %}
{% set def = 'number' %}
{% from 'macro/form/label.html.twig' import label %}
{{ label(name|default(def)) }}
<input name="{{ name|default(def) }}"
       type="{{ def }}"
       min="{{ min }}"
       max="{{ max }}"
       id="{{ id|default('name') }}"
       required>
</textarea>
{% endmacro number %}
