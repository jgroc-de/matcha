{% macro textarea(name, id, value) %}
{% set def = 'bio' %}
{% from 'macro/form/label.html.twig' import label %}
{{ label(name|default(def)) }}
<textarea name="{{ name|default(def) }}"
       value="{{ value }}"
       id="{{ id|default('name') }}"
       placeholder="raconte ta life">
</textarea>
{% endmacro textarea %}
